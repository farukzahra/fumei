# /recap

User invoked **`/recap`** or asked for a session delivery summary. Read and follow the **`recap`** skill (`~/.cursor/skills/recap/SKILL.md` or project `.agents/skills/recap/SKILL.md`).

Produce self-contained HTML: requested vs delivered, topic cards, dark mode. Save under `docs/session-recaps/` when the project has `docs/`.

Do not include secrets.
